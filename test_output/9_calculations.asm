; Código assembly gerado pelo compilador CafeLang
; Análise sintática concluída com sucesso

; push number 14.00
; variable declaration with init: dose_base
SET R0 14.00
; variable declaration: dose_total
; push number 30.00
; assignment: dose_total := <expr>
; Simplified: store result in memory location for dose_total
; variable declaration: temperatura_ok
; boolean true
; assignment: temperatura_ok := <expr>
; Simplified: store result in memory location for temperatura_ok
; Acao com argumentos
; Acao com argumentos
; Acao com argumentos
; === Fim da receita ===

; Fim do programa
HALT

; Programa analisado sem erros
